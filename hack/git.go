package hack

func Init() {
	/* Full git project init
			1. init
			2. main branch set to main
			3. creates user branch -> switches to user branch
			4. pulls from provided project
			To be integrated with the template pull 
	*/
}